language: go

sudo: required

services:
  - docker

install: true

script:
  - cd management/src
  - >
    make build &&
    make unit-test &&
    if [ "${TRAVIS_PULL_REQUEST}" = "false" ]; then
        make release;
    fi
